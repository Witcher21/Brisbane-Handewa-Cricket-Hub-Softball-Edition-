# ─────────────────────────────────────────────────
# Brisbane Handewa Cricket Hub — Backend .env
# DO NOT commit this file to version control!
# ─────────────────────────────────────────────────

# JWT — change this to a 32+ character random string in production!
JWT_SECRET_KEY=BrisbaneHandewa_SuperSecret_JWT_Key_2026!
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Allowed origins (comma separated)
CORS_ORIGINS=http://localhost:9000,http://localhost:9001,http://localhost:3000
